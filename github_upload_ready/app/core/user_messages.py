"""Shared user-facing copy strings (avoid circular imports between services)."""

# Appended or referenced when PDF processing fails — steers users to screenshot fallback.
PDF_SCREENSHOT_FALLBACK_HINT = (
    " Consider **Screenshot fallback** (PNG/JPEG): upload captures of the unpaid Schedule P table "
    "for packaged manual review — automated extraction from images is not enabled in this release."
)
